services:
  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    container_name: registration-swagger-ui
    ports:
      - "8080:8080"
    environment:
      SWAGGER_JSON: /docs/openapi.yaml
    volumes:
      - ./openapi.yaml:/docs/openapi.yaml:ro
    restart: unless-stopped
